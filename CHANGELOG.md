# Changelog

## 0.4.0

CodeLens、hover、自動補全。三個都讀**同一份判決**——`activate` 判完就存起來，
所以三個視角不可能互相矛盾，而且滑一下鼠標不會讓編譯器再跑一次。

**CodeLens：每個願望上方寫著它成不成。** `EXPLOIT · broke I2`、
`not granted · R1`、`clean`。這個語言唯一想回答的問題，掛在程式碼上。
讀不懂的檔案**不會**有 lens——沒有判決可報，而在放判決的位置放一句空話，
讀起來就是「乾淨」。

**Hover：說明是編譯器的,不是插件的。** 這需要動上游(編譯器 1.13.0)：
`--keywords` 本來只給 42 個保留字的名字，所以插件想顯示 `sub` 的意思
就得自己帶一份散文——那是語義的第二份說法，可以自由地跟本體漂走而沒人會發現。
現在 `--keywords` 帶 `docs`，每個字一句話加一行語法，
而 operation 的語法是從它宣告的 `OperandKind` **生成**的，
不可能出現「文件說吃寬度、parser 要立即值」。

滑到 register 上會顯示它**在每個願望之後**的值：

```
wishes  uint<64>
after each wish: 3 → 2 → 18446744073709551615
```

**自動補全。** 看上下文（只看當前這一行，這是啟發式，不是 parser——
真的 parser 在編譯器裡，而編譯器沒辦法解析游標下那行寫一半的東西）：
`sub ` 後面給 register，`kill ` 後面給人，行首給所有能開頭的東西。
名字優先從判決來；檔案不能判的時候退回讀原始碼——**而那正是有人在打字的時候**。

抓到一個 bug 值得單獨講。做 register hover 的時候發現
`18446744073709551615` 顯示成 `18446744073709552000`——`JSON.parse` 用浮點
把 uint64 吃掉了。這不是插件的問題，是**契約的問題**：任何 JS 消費者讀
register 值都會靜默拿到錯的數字，而且錯的正好是這個語言存在的理由那個數字。
編譯器 1.14.0 改成把值當字串傳，並且附上**當下的**寬度
（寬度不是常數，`widen` 會改它——從原始碼刮宣告值的話，
一個已經 64 位元兩個願望的 register 會被顯示成 `uint<2>`）。

## 0.3.0

**一個單獨開著的 `.genie` 現在會即時檢查語法。** 在這之前，精靈檔案裡的語法錯誤
是靜默的——你在慢慢調規則、還沒寫任何願望，打錯一個字（少個括號之類）
編輯器一聲不吭，要到某個願望去引用它才會冒出來。現在它當場劃線。

只查語法，這是刻意的。精靈的不變量可以引用一個 register，
而那個 register 存不存在要看願望裡的世界——光看精靈檔案不知道。
所以這個檢查解析精靈、報告它是否合語法，然後就停。

順手把診斷的歸屬理乾淨了。**每個檔案只負責畫自己的線：**

- `.genie` 的語法錯誤畫在精靈自己身上。
- `.wish` 引用到一個讀不懂的精靈時，只在願望的 `# genie:` 那行放一個 warning
  說「沒辦法判」，把精確的錯誤留給精靈檔案自己。**它不會再把精靈的錯誤
  用精靈的行號劃在願望的程式碼上**——那是舊版在精靈沒開著時會做的事，
  非常有自信地指著一段完全沒問題的願望。

上游為此加了 `--check-genie`（編譯器 1.12.0）和嵌入式的 `checkGenie`。

## 0.2.0

行內診斷。套件現在內含編譯器本體（WebAssembly，367 KB），
所以判決來自編譯器自己，不是另外寫一套規則放在編輯器裡——
那種東西遲早會跟本體不一致，然後同一個語言會因為你在哪裡讀而有兩種意思。

**破掉不變量不算錯誤。** 這是整個功能唯一真正的設計決定，而它跟直覺相反：

| | 顯示成 |
| --- | --- |
| 讀不懂這個檔案 | `Error`。什麼都沒被判，這裡唯一真正的問題 |
| 精靈拒絕了 | `Information`。合法的結果，規則正常運作 |
| `EXPLOIT` | `Information`。你贏了 |
| 什麼都沒破 | 不顯示 |

在這個語言裡寫出一個合規卻拆穿精靈的願望就是目標。
把它算進錯誤數，等於編輯器在騙你說你做壞了。
所以那個紅色數字只數一件事：編譯器讀不懂的檔案。

其他：

- `# genie: mine.genie` 決定用哪個精靈判，跟編譯器 `make run` 同一個慣例。
  **還沒存檔也算**——編輯精靈的時候，願望那邊的波浪線會跟著動，
  不然兩個分頁會在螢幕上對同一個時刻各說一套
- 精靈裡的語法錯誤劃在精靈上。少了這個路由，錯誤會用精靈的行號劃在願望上，
  非常有自信地指著一段完全沒問題的程式碼
- 空的願望也有地方放判決。`wish w {}` 沒有任何語句，
  而四個空願望會讓過路費下溢——這是這個語言裡比較利的一招，
  卻是唯一沒有語句可以指責的一招
- 打字後 250 ms 才判

上游為此加了三樣東西（編譯器 1.10.0 和 1.11.0）：
`--json` 現在會回報錯誤的位置（在那之前位置只存在於散文報告裡，
而規格 §10.1 明講散文可以隨時改寫）、每個願望帶自己的行號、
拒絕帶結構化的規則名和行號。

## 0.1.0

第一版：`.wish` 和 `.genie` 的語法高亮。

**文法是生成的，不是手寫的。** 哪些字是保留字由編譯器決定——
`loophole --keywords` 從它的 lexer 和 parser 讀的同一張表產生輸出，
隨每個 release 發布成 `keywords.json`，這個套件從那份檔案生成兩份 TextMate 文法。
所以語言長出新的保留字時，這裡不會靜默地停止上色。

- 兩種語言各一份文法，`.wish` 和 `.genie`
- `uint<N>` 的寬度單獨上色——那個數字是整個笑話的來源
- `define` 綁的別名跟真正的操作同色。別名軸就是「名字和它指的東西不是同一件事」，
  編輯器把它顯示成一樣，是在說實話
- `written` 和 `real` 有自己的 scope，因為那兩欄的落差是這個語言的核心
- 註解、字串、括號配對、縮排

用 `vscode-textmate` 和 `vscode-oniguruma`（VS Code 本身的 tokenizer）測試，
不是只驗 JSON 合法——一份文法可以完全合法卻讓每個關鍵字都沒顏色。
